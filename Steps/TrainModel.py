import os
import logging
import pandas as pd
from sklearn.tree import DecisionTreeRegressor
from xgboost import XGBRegressor
from catboost improt CatBoostRegressor
from lightgbm import LGBMRegressor
from sklearn.



class model_training:
    def __init__(self,x_train, x_test, y_train, y_test) ->tuple(pd.DataFrame,pd.Series):
        self.x_train=x_train
        self.x_test=x_test
        self.y_train=y_train
        self.y_test=y_test
        pass
    
    def DecisionTreeRegressor(self,x_train, x_test, y_train, y_test)->tuple(pd.DataFrame,pd.Series):
        
        
    def model_selection(self)-> None:
        
        
        
        
   
        
        
        
        